{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.frontend"
  },
  "deploy": {
    "startCommand": "serve artifacts/web-app/dist -p 3000",
    "restartPolicyType": "ON_FAILURE"
  }
}
